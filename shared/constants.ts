// Re-exports of the constant arrays from types.ts so consumers can pull
// runtime values from a dedicated path if they prefer.
export {
  CATEGORIES,
  MODULE_STATUSES,
  PRICING_MODELS,
  COMPLEXITY_LEVELS,
} from './types';
